/**
 * Detalle operativo de certificado — Escuelas Normales.
 * Reutiliza revisión institucional (misma lógica, ruta canónica separada de UPN).
 */
import { RevisionInstitucionalPage } from '../documentos/RevisionInstitucionalPage';

export function NormalesCertificadoDetallePage() {
    return <RevisionInstitucionalPage />;
}
